name: Test

on: [push]

jobs: 
    test-build:
        run-on: ubuntu-latest 

        steps:
        - name: Checkout
          uses: actions/checkout@v4.1.7
        
        - name: lista
          runs-on: ls
         #    chmod +x script.sh
         #   ./script.sh